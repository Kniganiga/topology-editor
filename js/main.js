import { lineComponent } from './components/line.component.js';
import paper from 'paper';

paper.setup(document.querySelector('#window'));

// lineComponent({ start: [0, 0], end: [200, 200], color: 'red' });
